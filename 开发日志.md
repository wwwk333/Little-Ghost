# 末日求生 · 开发笔记

同时是改动日志和操作手册。找东西按目录跳。

- [一、怎么写剧情](#一怎么写剧情)
- [二、怎么调试](#二怎么调试)
- [三、改动日志](#三改动日志)
- [四、待办](#四待办)

---

## 一、怎么写剧情

所有内容都在 `末日求生_第一章.html` 一个文件里。写剧情只碰 `const NODES = {` 那一段，配置在它上面。

### 1. 一个最简单的节点

```js
"c1_例子": {
  text: `<p>这里是正文。</p>
         <p>一段一个 &lt;p&gt;，玩家点一下出一段。</p>`,
  choices: [
    { key:"A", text:"选项文字", next:"下一个节点id" },
  ]
},
```

**不需要写的东西**（引擎自动处理）：章节名、背景图、背景音乐、节点属于第几章。节点 id 以 `c1_` 开头就自动归入第一章，音乐和背景从 `CHAPTERS.c1` 取。

### 2. 文字样式

| 写法 | 效果 |
|---|---|
| `<p>普通</p>` | 正文 |
| `<p class='narr-scene'>…</p>` | 灰色斜体，环境描写 |
| `<p class='narr-thought'>…</p>` | 米白斜体，内心话 |
| `<p class='narr-important'>…</p>` | 偏红加粗，重点 |
| `<p class='narr-hint'>【…】</p>` | 黄铜色带左边线，给玩家的提示 |

### 3. 音效

在文字里插标记，打字机打到那个字才播：

```js
text: `<p>[[sfx:knock]]"咚咚咚！"有人敲门。</p>
       <p>突然，"啊！"[[sfx:scream]]楼上传来一声尖叫。</p>`,
```

段首就是段落一开始播，插在句中就是打到那个字才播。可用的名字在 `const SFX` 里：

`tv` `clock` `phone` `knock` `scream` `drip` `steps` `info`

加新音效：把 mp3 放进项目根目录，在 `SFX` 里加一行。

**`info` 是情报提示音**，四个信息点已经全部挂上，写在【获得逃生信息+1】那一行的行首：

| 节点 | 拿到的情报 |
|---|---|
| `c1_eye_cheek` | vision + eastgap |
| `c1_sun_success_night` | light |
| `c1_loud_noise_safe` | sound |
| `c1_to_door` | sound |

以后新增信息点，除了在 `INTEL_SOURCES` 登记，别忘了正文里补 `[[sfx:info]]`。两处是独立的：`INTEL_SOURCES` 管情报真的入库，`[[sfx:info]]` 只管响声。

### 4. 概率判定

概率节点只写档位，不写数字：

```js
"c1_例子_roll": {
  isRoll: true,
  tier: "mid",              // low 凶险 / mid 五五 / high 稳妥
  successNext: "成功后的节点",
  failNext:    "失败后的节点",
},
```

要整体调难度，改 `ROLL_TIERS` 那三行区间，全部节点同步生效：

```js
const ROLL_TIERS = {
  low:  { label:"凶险", range:[0.28, 0.42] },   // 期望 ≈35%
  mid:  { label:"五五", range:[0.48, 0.62] },   // 期望 ≈55%
  high: { label:"稳妥", range:[0.72, 0.86] },   // 期望 ≈80%
};
```

每次判定会在区间内随机取一个阈值再掷骰，所以同一档每次的实际成功率也有浮动。

**让装备和同伴影响成功率**（`tierIf` 从上往下第一条命中的生效）：

```js
"c1_girl_rescue_roll": {
  isRoll: true,
  tier: "low",                                    // 空手：凶险
  tierIf: [
    { item:"斧头", has:"老孙", tier:"high" },      // 有武器 + 有帮手
    { item:"斧头",            tier:"mid"  },      // 只有武器
    { has:"老孙",             tier:"mid"  },      // 只有帮手
  ],
  successNext: "…", failNext: "…",
},
```

### 5. 条件判定：所有能用的条件

下面这些字段能用在 **三个地方**：`next` 分支、条件文本块、选项的 `requires`。

| 字段 | 含义 |
|---|---|
| `has:"老孙"` | 队伍里有这个人 |
| `notHas:"老孙"` | 队伍里没有 |
| `item:"斧头"` | 持有物品 |
| `notItem:"斧头"` | 没有该物品 |
| `knows:"sound"` | 已掌握某条情报 |
| `notKnows:"sound"` | 尚未掌握 |
| `intelMin:2` | 情报条数下限 |
| `flag:"night"` | 剧情开关已置 |
| `notFlag:"night"` | 剧情开关未置 |
| `seen:"节点id"` | 玩家读过某节点 |

多个字段写在一起 = 全部满足才算命中。

### 6. 分线只差一两句话：条件文本块

**不要为了一句台词复制整个节点。** `text` 可以写成数组：

```js
text: [
  `<p>大家都一样的开头。</p>`,

  // 二选一
  { has:"老孙",
    text: `<p>老孙也跑出来了。</p>`,
    else: `<p>你独自僵在原地。</p>` },

  // 满足才插入，不满足整段不出现（没有 else）
  { flag:"night", text:`<p>夜里的额外描写。</p>` },

  `<p>大家都一样的结尾。</p>`,
],
```

现在 `c1_girl_event` 一个节点覆盖了「单人/有老孙 × 白天/夜晚 × 知情/不知情」八种组合。以前这是三个节点复制粘贴。

### 7. 状态变化

```js
stateChange: {
  water: "充足",           // 水源，直接赋值
  food:  "不足",           // 食物
  join:  "老孙",           // 加入队伍（也可以 ["老孙","林晓雨"]）
  leave: "老孙",           // 离队
  item:  "斧头",           // 获得物品
  itemLose: "菜刀",        // 失去物品
  intel: ["sound"],        // 掌握情报
  flag:  "night",          // 打剧情开关
  mood:  "scared",         // 主角表情
  condition: -35,          // 身体状况增减（0~100）
},
```

**情报更推荐在 `INTEL_SOURCES` 里登记**，不用写进节点：

```js
const INTEL_SOURCES = {
  c1_eye_cheek:       ["vision", "eastgap"],   // 这个节点给两条情报
  c1_loud_noise_safe: ["sound"],
};
```

文字里写了「强力武器+1」会自动获得斧头，规则在 `AUTO_STATE_RULES`。

### 7b. 装备耐久

登记在 `ITEM_DURABILITY` 里的装备才有耐久条：

```js
const ITEM_DURABILITY = {
  "斧头": { max: 6, breakText: "斧头彻底崩了刃，再也砍不动了。" },
};
```

剧情里让它掉耐久，**在正文里写一行就行**，不用碰 `stateChange`：

```js
text: `<p>斧头落下，血花飞溅……</p>
       <p class='narr-sys'>斧头耐久 -2</p>`,
```

同一节点只结算一次（跟「强力武器+1」一样的机制）。左栏物品栏和人物状态面板都会显示 `4/6` 和一条小血条，低于 1/3 转黄、剩 1 点转红。

**扣到 0 自动损坏**：移出背包（`requires:{item:"斧头"}` 随之锁住、`notItem:"斧头"` 的分支开始成立），物品栏里留一条划掉的「已损坏」记录，并弹一句 `breakText`。重新拿到同名装备耐久回满、损坏记录消失。

> 注意：因为损坏会改变背包内容，而条件文本块（`{ item:"斧头", text:…, else:… }`）依赖背包判断，所以引擎在状态结算时就把正文**定稿**一次（`_composedText`）。这样"这一节点里砍断"的情况下，正文仍是"有斧头"那一版，不会前后文自相矛盾。

### 8. 情报：加一条新线索

```js
const INTEL_DB = {
  你的key: {
    icon: "🔧", title: "标题（左栏面板里显示）",
    detail: "玩家点开能看到的正文",
    use: "用途一句话",
  },
};
```

然后在 `INTEL_SOURCES` 里指定哪个节点给它。玩家点左栏「逃生情报」那一行就能查阅。

### 9. 锁线：情报/装备决定选项能不能选

```js
choices: [
  { key:"A", text:"冲出去救她", next:"c1_girl_rescue_roll" },

  { key:"B", text:"制造点声音，把那些东西引开。",
    next:"c1_girl_lure_roll",
    requires: { knows:"sound" },        // 没这条情报就锁住
    lockText: "（你想不出别的办法。）",   // 锁住时显示的文字
    lockHint: "缺少情报" },              // 右侧小标签
]
```

锁住的选项**会显示**，灰掉且不可点，让玩家知道「这里本来有条路」。键盘数字键也绕不过去。

不想让玩家知道选项存在，用 `hidden` 代替 `requires`，条件不满足时整条不渲染。

### 10. 情绪：默认自动判断

**平时不用写。** 引擎按当前状态自己挑表情，规则在 `MOOD_RULES`，从上往下第一条命中的生效：

```js
const MOOD_RULES = [
  { condMax: 30, mood: "hurt"   },                   // 伤重
  { condMax: 55, mood: "tired"  },                   // 掉了一半血
  { flag: "night", notHas: "老孙", mood: "scared" },  // 夜里独自一人
  { flag: "danger",             mood: "scared" },    // 剧情打了 danger 标记
  { has: "老孙",                 mood: "calm"   },    // 有人搭伴
  { item: "斧头",                mood: "calm"   },    // 手里有家伙
];
```

条件写法和 `requires` 一样（`has` / `item` / `knows` / `flag` / `notXxx`…），额外支持 `condMax` / `condMin` 判体况。都不命中就是 `normal`（显示「还算镇定」）。

想在某处**锁定**表情：

```js
stateChange: { mood: "scared" }   // 一直锁到下次改写（写进存档）
heroMood: "scared"                // 只影响当前节点（不写进存档）
stateChange: { mood: "auto" }     // 交还给自动判断
```

可用表情：`normal` `calm` `scared` `hurt` `angry` `tired`。在 `HERO.faces` 里登记图片路径和 **中文说法**：

```js
scared: { img: "images/hero_scared.png", icon: "😨", label: "惊恐" },
```

`label` 是人物状态面板里显示的文字 —— 面板不显示 `normal` 这种内部名。图放进 `images/` 就自动生效；没有图会显示姓氏字兜底，不会破图。

**队友情绪**同样自动判断，在 `CAST` 里写 `moodRules`：

```js
"老孙": {
  mood: { icon: "😟", label: "担忧" },          // 默认
  moodRules: [                                  // 命中就覆盖默认
    { flag: "night", icon: "😖", label: "强撑着" },
    { has: "林晓雨", icon: "🙂", label: "稍微安心" },
  ],
},
```

---

## 二、怎么调试

### 存档面板怎么用

一个面板两种模式，标题会跟着变：

- **读取存档** — 点一下读档。空位置灰显示「无存档」，不可点
- **保存存档** — 点一下写入，已有内容会被覆盖。空位显示「写入」

进入路径：HUD 的 💾 默认开在读取模式；`S` 键直接开在保存模式；「保存后返回首页」走 `save_then_home`，存完自动回标题页。

✎ 重命名、✕ 删除只出现在**有内容的存档**上，两种模式都有。自动存档只读，不能写入，但可以删。

标题页点「读取存档」也是同一个面板。抽屉挂在 `#app` 里、层级比标题页低，所以会临时把标题页压到 `#app` 下面并挂 `from-title` 藏掉游戏内 HUD，关掉时 `restoreTitleLayer()` 还原 —— 改这块要留意 `openDrawer` 开头的 `closeDrawer(true)` 不能触发还原。

### 快捷键

| 键 | 作用 |
|---|---|
| `Ctrl+Shift+D` | **开关测试面板** |
| `Ctrl+Shift+L` | **弹出独立日志窗口** |
| `I` / `R` / `S` / `F` | 情报 / 回顾 / 存档 / 反馈 |
| `M` | 静音 |
| `H` | 返回首页 |
| `1` `2` `3` … | 直接选对应选项 |
| `Esc` | 关闭当前面板 |

### 测试面板（Ctrl+Shift+D）

- **节点列表** — 搜 id 也能搜正文，点一下直接跳过去。标注「概率 / 好结局 / 坏结局 / 读过」
- **状态篡改** — 水、食、情报、队伍、物资、剧情开关、情绪、体况，改完点「应用状态」
  - 情报填 key，逗号分隔：`sound,vision,eastgap,light`
  - 开关填：`night`
  - 物资填名字；带耐久的（如斧头）会自动补成满耐久
  - 情绪选「自动判断」= 交给 `MOOD_RULES` 现算，选具体一档 = 锁定
- **测试概率** — 按使用顺序排：判定点 → 本点档位 → 各档区间 → 掷骰子 / 测试抽样 → 保存修改 / 恢复默认 / 生成代码，见下节
- **掷骰（两层）** — 「档位」层强制用某一档的区间，「结果」层跳过随机直接给成败，见下节
- **节点连通检查** — 一键扫断链、空 next、孤儿节点、未登记音效
- **回顾全解锁** — 把所有剧情节点标记为已读、写进「剧情回顾」列表。用途是**直接跳到任意一段校对文字**，不用重头再打一遍

### 测试概率：五步顺下来

1. **判定点** — 选要测哪个概率节点。括号里是它当前实际用的档位（有 `tierIf` 改档时显示 `low→high`）
2. **本点档位** — 只改这一个判定点的 `tier`。下面的状态条会说清：改自源码哪一档、有没有被强制档位盖掉、有没有命中 `tierIf`
3. **各档区间** — 改 low/mid/high 的百分比区间，点「应用区间」立刻作用于全部判定
4. **掷骰子 / 测试抽样** — 掷骰子看单次结果；测试抽样跑 N 次对比实测与期望（会把 `tierIf` 提档算进去，临时忽略强制成败）
5. **保存修改 / 恢复默认** — 保存把「各档区间 + 各判定点的档位」一起写进 localStorage，刷新自动套用；恢复默认两样一起回到源码值并清掉保存。想写回源码点「生成代码」

> 「本点档位」和「流程」里的**强制档位**不是一回事：前者只改选中的那一个判定点，后者把所有判定都钉成同一档（临时手感测试用，`tier` / `tierIf` 全部失效）。

### 掷骰流程：一次判定分四步

代码在 `rollTier()`。选项点下去以后：

**① 定档** — `resolveTier(node)` 从上往下扫 `tierIf`，第一条命中的生效；都不命中用节点的 `tier`。

**② 定这次的阈值** — 档位给的是**区间**不是定值：`threshold = lo + random()*(hi-lo)`。high 是 `[0.72, 0.86]`，所以这次可能是 78.3%，读档重来可能是 81.5%。这一层让同档位每次手感不完全一样。

**③ 掷骰** — `dice = random()`，`dice < threshold` 即成功。

**④ 分流** — 成功走 `successNext`，失败走 `failNext`；两边都支持 `{ has:"老孙", then, else }` 再分一次。

日志那行就是这四步：

```
[ROLL] high 区间=72~86% 阈值=0.783 骰=0.412 → 成功
```

### 掷骰的两层开关：档位 vs 结果

面板「流程」区是两个独立下拉，可以叠着用：

| 开关 | 改的是 | 还掷骰吗 | 用来干什么 |
|---|---|---|---|
| **档位** | 这次判定按哪一档算 | 掷 | 验单档手感，不用真去背包塞斧头 |
| **结果** | 直接给成功/失败 | 不掷 | 稳定复现某条分支，过剧情 |

**档位**选了强制以后，节点的 `tier` 和 `tierIf` 暂时失效。选「强制 high」就能试有斧头是什么感觉，选「强制 low」试无斧头版剧情——省掉在状态篡改里反复加减斧头。被钉住的那一档在档位编辑器里会整行提亮加 `◀`。

**叠用举例**：强制 high + 强制失败 = 按稳妥档走剧情走向，但结果判失败，专门看 `failNext` 那条线。

面板下方状态条实时显示组合结果（`掷骰：强制 high（稳妥），正常随机`），关掉再打开面板下拉框会回填当前值，不会显示和实际不一致。

### 概率档位（区间数值）：调整到底生不生效？

**生效，而且作用在真实判定上。** 面板里改的就是 `ROLL_TIERS` 本身，不是另开一套摆设。可以自己验证：把 low 改成 `0~0`，任何 low 判定都必失败；改成 `100~100`，必成功。

要分清三种"生效范围"，状态条会明确告诉你现在是哪种：

| 操作 | 效果 | 刷新后 |
|---|---|---|
| **应用** | 立刻作用于本局所有判定 | ✗ 回到源码值 |
| **存为默认** | 同上，且写进浏览器 | ✓ 自动套用 |
| **生成代码** | 复制一段 `ROLL_TIERS = {…}` 到剪贴板 | — 你手动粘回源码 |

完整流程：**面板里试参数 → 抽样验证手感 → 满意了点「生成代码」→ 粘回 HTML 里的 `ROLL_TIERS`。** 这样调试结果就真正落进代码，不再是"只在我这台机器上对"。

状态条颜色：灰=源码原值，黄=改了但只在本局有效，绿=已存为默认。

### 概率抽样验证

选判定点、选次数、点「开始」，用**真实的 `rollTier`** 跑 N 次，给你：

- 实测成功率 vs 期望值（进度条，黄竖线是期望刻度）
- 命中 `tierIf` 改档时显示 `low → high` 提档过程——**看到的就是玩家实际面对的概率**，不是纸面档位
- 成功/失败各跳到哪个节点

想测"有斧头时救人成功率"，先在状态篡改里加斧头再抽样，档位会自动提到 high；或者直接用「档位」下拉选强制 high，更快。

两个强制开关在抽样里的待遇不一样：

- **强制结果** — 抽样时**临时忽略**（否则测出来永远 100%/0%），结果里有提示
- **强制档位** — 抽样时**照算**，因为你就是想看那一档的实测率。输出会标明「测试面板强制档位，tierIf 已被忽略」，不会让你误以为测的是玩家实际面对的档

判定点下拉里的标注也跟着变：`c1_girl_rescue_roll（low→high）` 表示纸面 low、当前实际 high。

### 独立日志窗口（Ctrl+Shift+L）

面板里的小日志框太挤，按 `Ctrl+Shift+L` 弹出独立窗口（或点面板里「弹出独立窗口」）：

- **拖标题栏**移动，**拖右下角**改大小
- `A+` / `A−` 调字号，`↵` 切自动换行
- 位置、大小、字号都记住，下次自动恢复
- 日志按类型上色：掷骰(黄) / 状态(绿) / 错误(红) / 时间戳(灰)

可以拖到屏幕一侧，边玩边看每次判定的阈值和骰子值。

### 命令行自检

改完剧情，**双击根目录 `检查.bat`**，或：

```bash
node _tests/run_all.js
```

四步依次跑：

| 步骤 | 查什么 | 什么时候会红 |
|---|---|---|
| ① 语法与结构 | JS 还能不能跑 | 少个逗号、括号没配对 |
| ② 剧情连通性 | 断链、节点名打错、音效没登记、**还剩多少 TODO** | `next` 指向不存在的节点 |
| ③ 界面引用 | 按钮点了会不会报错 | onclick 调了不存在的函数 |
| ④ 功能回归 | 206 项：引擎、存档、概率、分支、情报、主角卡、人物档案、日志窗 | 引擎被改坏了 |

日常写剧情，**②最有用**。节点名和音效名打错是最容易犯又最难自己发现的——游戏里表现就是「点了没反应」或「音效不响」。

注意 `_dom_check.js` 用的是相对路径 `../末日求生_第一章.html`，单独跑它必须先 `cd _tests`，否则报 ENOENT。`run_all.js` 和 `检查.bat` 已经处理好了，直接跑没问题。

### 当前基线

```
② 剧情连通性：1 个错误、0 个提醒、9 个节点待写正文
   错误就是 c1_ending → c2_start（第二章还没写）
④ 功能回归：206 项全过
```

**跑出来多于这个数就是新踩的坑。**

### 单独跑某一步

```bash
cd _tests
node _integrity.js      # 只查剧情数据（最常用）
node _runtime_test.js   # 只跑功能回归
node _dom_check.js      # 只查界面引用
python _check.py        # 只查语法
```

### 改了引擎之后

`_tests/_runtime_test_body.js` 里是断言，跟引擎行为绑在一起。如果你故意改了某个机制的行为，对应断言会红——那时候要改的是断言，不是引擎。

断言尽量写成**结构性**的，比如「掌握情报前后文本不同」而不是「文本等于某句话」，这样你重写正文不会导致测试假红。

---

## 三、改动日志

### 第五轮（本次）

- **版本号改 v1.0**：标题页右下角从 `v2.0` 改成 `v1.0`，当版本号用。开创作者模式还是连点它 5 次
- **标题页只留两个入口**：去掉「结局收集」（左栏 HUD 里那个入口还在，游戏内照常看）
- **修掉标题页读档点了没反应**：抽屉挂在 `#app` 里（`z-index:10`），标题页是 `z-index:100`，原来只把标题页压到 `20` —— 还是盖在抽屉（`40`）上面，所以点了像没反应。现在压到 `5`，再用 `#app.from-title` 把游戏内 HUD（顶栏/左栏/剧场/立绘/右下角按钮）全藏掉，只留抽屉浮在标题画面上；关掉抽屉时 `restoreTitleLayer()` 还原。注意 `openDrawer` 开头会 `closeDrawer(true)` 清场，所以还原只在非 silent 时做，否则刚挂上就被撤掉
- **斧头加耐久 6 点**：新增 `ITEM_DURABILITY` 登记表，登记过的装备才有耐久。剧情里照常写一行 `<p class='narr-sys'>斧头耐久 -2</p>`，`AUTO_STATE_RULES` 自动扣（同一节点只结算一次）。左栏物品栏和人物状态面板都显示耐久条与数字，低于 1/3 转黄、剩 1 点转红。扣到 0 自动损坏：移出背包（`requires:{item:"斧头"}` 随之失效、`notItem` 分支成立），物品栏留一条划掉的「已损坏」记录。重新拿到同名装备耐久回满
  - 顺带修了一个隐患：耐久归零会把装备移出背包，而条件文本块是渲染时才 compose 的，于是同一节点会前脚写「斧头落下」后脚翻成「一刀砍在」。现在 `applyNodeState` 把正文定稿一次存进 `_composedText`，`renderNode` 直接用，不再二次 compose
- **测试概率面板按使用顺序重排**：判定点 → 本点档位 → 各档区间 → 掷骰子 / 测试抽样 → 保存修改 / 恢复默认 / 生成代码，后面才是工具（节点连通检查、打印状态、回顾全解锁）和运行日志。「本点档位」是新的：只改选中那一个判定点的 `tier`，和「流程」里的强制档位不是一回事（后者把所有判定钉成同一档）。「保存修改」把档位区间和各判定点的档位一起存进 localStorage，刷新自动套用；「恢复默认」两样一起回源码值
  - 「回顾全解锁」就是原来的「解锁全部回顾」：把所有剧情节点标记为已读、写进「剧情回顾」列表，这样能直接跳到任意一段校对文字，不用重头打一遍。按钮上加了 `title` 说明
- **存档不再说槽位数**：「手动存档 · 5 槽」改成「手动存档」，面板说明里的"槽位/空槽"也改成"存档/空位"
- **情绪自动判断**：新增 `MOOD_RULES`，按当前状态现算表情（伤重 → 受伤，掉一半血 → 疲惫，夜里独自一人 → 惊恐，有老孙或有斧头 → 平静，都不命中 → 还算镇定）。`gameState.mood` 默认 `"auto"`；剧情写 `stateChange:{mood:"angry"}` 仍可锁定，写 `"auto"` 交还自动判断。队友同理，`CAST` 里加 `moodRules`
  - **情绪显示改成文字**：`HERO.faces` 每档加 `label`，人物状态面板的「当前情绪」不再显示 `normal` 这种内部名，改成「还算镇定」「惊恐」等，自动判断时标注"（随状态变化）"。队友档案也新增「当前情绪」一栏。`CAST.mood` 从纯 emoji 改成 `{icon,label}`（旧写法仍兼容）
  - 顺带修正：节点级 `heroMood` 原本直接写 `gameState.mood`，和它自己"不写进存档"的注释矛盾，也会残留到下一节点。现在走独立的 `_nodeMood`，只管当前节点
- **测试**：新增装备耐久 19 项、情绪自动判断 20 项、标题页层级还原 8 项。同时修了一批早前遗留的测试漂移（引用已删节点 `c1_girl_rescue_roll`、已改名的 `devFillSampleNodes`/`dev-sample-*`、已移除的 `setSaveMode` 和读取/保存页签、槽位数 6→5、存档要过 confirmBox 但测试没人点确认）。测试 206 → **262 项**
  - 仍有 1 项失败，是**剧情没写完**、不是代码问题：`c1_girl_saved_plan` 两个选项 `next` 是空的，对应的 `c1_girl_saved_plan_voice` / `c1_girl_saved_plan_window` 写好了但没接上（另有 `c1_die_girl_rescue` 也不可达）

### 第四轮

- **信息点音效**：`SFX` 加 `info`（`information.mp3`，vol 0.42，6 秒淡出，压得比尖叫/敲门轻）。四个信息点正文里挂上 `[[sfx:info]]`：`c1_eye_cheek`、`c1_sun_success_night`、`c1_loud_noise_safe`、`c1_to_door`
- **救林晓雨的档位简化**：常规武器（水果刀/菜刀）每条线都有，包括老孙被动加入那条，所以不再进判定。`tierIf` 从四条规则砍到一条 —— **只看斧头**：有斧头 `high`，没有 `low`。老孙在不在只影响失败时你死不死（`failNext` 没动）
- **成功剧情拆两份**：原 `c1_girl_saved` 拆成 `c1_girl_saved_sun`（黑夜线/白天线/被动加入 三线共用，内部只留一处"有斧头/无斧头"对照）和 `c1_girl_saved_solo`（单人专属，保留夜/昼对照，因为单人线也可能走到夜里）。分流写在 `successNext: { has:"老孙", then:…, else:… }`
- **HUD 去掉天数**：夜晚线时间轴跨到第二天，写死 `DAY 1` 自相矛盾。`CHAPTERS.sub` 只留地点（`城东老小区` / `小区外围`）
- **掷骰拆成两层**：新增「档位」下拉（强制 low/mid/high，`tier` 和 `tierIf` 暂时失效），和原有「结果」下拉（强制成功/失败）独立可叠。抽样验证会照算强制档位、临时忽略强制结果。被钉住的档位在编辑器里整行提亮加 `◀`
- **修掉手动存档点了没反应**：`openDrawer("save")` 无条件把 `saveMode` 重置成 `"load"`，HUD 的 💾 走的就是这条，所以面板永远是读取模式；读取模式下空槽不挂 `onclick`，点了当然没动静。现在面板顶部加「读取 / 保存」切换条，`openSaveModal` 指定过的模式用 `_saveModeExplicit` 标记保住不被冲掉。读取模式的空槽加 `.dead` 置灰并显示「无存档」，不再装作能点。标题页进来时藏掉切换条（还没开局，只能读）
- **测试同步**：`resolveTier` 的三条旧断言按新规则重写（有斧头 → `high`、只有老孙 → `low`、递增检查改成"斧头提档 + 老孙不提档"两条）。新增强制档位 5 项、存档模式切换 14 项。测试 186 → **206 项，0 失败**

### 第三轮

- **顶栏**：返回首页移到左组，右组只剩设置和反馈
- **立绘放大**：右下角 NPC 立绘 300→420px，高度 74vh→88vh
- **左栏放大**：214→268px，头像 62→84px，字号整体上调
- **逃生情报去掉固定格子**：原来画 6 个小方块暗示"总共 6 条"，现在改成"有情报就整行亮起 + 报条数"，以后加多少线索都不用改 UI，也不剧透总数。情报面板同理，只列已掌握的，不再显示 `？？？` 占位
- **队伍成员可点开**：左栏每个队友一行带小头像，点击查看人物档案（立绘、身份、你和他的关系、当前状态、标签、备注）。主角卡也可点，看自己的体况/情绪/物品/情报汇总。档案面板底部能在队伍成员间切换。数据在 `CAST` 里，加字段就加内容
- **概率调整真正闭环**（回答"是摆设吗"）：测试面板能直接改三档区间，立刻作用于真实判定；可「存为默认」跨刷新保留；可「生成代码」复制 `ROLL_TIERS` 片段粘回源码。状态条三色明确区分"源码值/本局临时/已存默认"
- **概率抽样验证**：选判定点跑 N 次，实测成功率对比期望值，还原玩家实际面对的概率（含 `tierIf` 提档）
- **独立日志窗口**（Ctrl+Shift+L）：可拖动、可缩放、可调字号、可切换换行，位置记忆，按类型上色。解决"面板日志太小看不清"
- 测试从 152 → 186 项

### 第二轮

**① 修掉过场的全屏粒子**
噪点 SVG 只有 220×220 却被拉伸到视口 140%，每个噪点像素放大十几倍，所以是大颗粒。改成固定 128px 平铺、透明度 0.14→0.06，扫描线也调细。

另外引导页淡出的 900ms 里标题页还是 `display:none`，露出了下面的舞台滤镜层——这就是你看到的「中途插入一个全屏特效」。现在改成先把标题页垫在下面再淡出引导页，中间一帧都不露。舞台滤镜层整组只在游戏内显示。

**② 逃生情报可查阅**
情报从一个数字改成条目集合。四条线索登记在 `INTEL_DB`：

| key | 内容 | 从哪拿到 |
|---|---|---|
| `sound` | 它们靠声音定位 | 大声呼救侥幸活下来 / 老孙隔门告知 |
| `vision` | 它们视野很窄 | 窗边观察 |
| `eastgap` | 小区东侧有缺口 | 窗边观察 |
| `light` | 光源会吸引它们 | 夜里用手电做实验 |

左栏「逃生情报」那行可以点开，已掌握的显示标题+正文+用途，未掌握的显示灰色 `？？？` 占位。拿到新情报时图标会呼吸提示，点开后消失。

哪个节点给哪条情报写在 `INTEL_SOURCES`，不用改剧情文字。

**③ 分线只差一两句 → 条件文本块**
新增 `composeText`，`text` 可以写成数组，差异部分写成条件块。顺手合并了 5 个重复节点：

- `c1_main_girl` + `c1_sun_person_daytime` + `c1_sun_person_night_girl` → **`c1_girl_event`**（三个节点 90% 重复）
- `c1_sun_person` + `c1_sun_person_night` → **`c1_sun_join`**（两个节点 95% 重复，只差「今晚」和一句没买菜）

夜晚线靠 `stateChange: { flag:"night" }` 打标记，文本自动切换。改一次台词四条线同时生效。

**④ 林晓雨事件重做**
- A 冲出去救：**不锁线**，谁都能选。空手 `low`，有斧头 `mid`，斧头+老孙 `high`
  - 失败且有老孙 → 老孙拖你回来，重伤但活着
  - 失败且孤身 → 死亡结局
- B 制造响动引开：**要先掌握 `sound` 情报才解锁**，固定 `low`（大概率救不下来）
- C 不管：我擅自加的第三条出路，不想要整条删掉就行

实测成功率随装备递增，测试里有断言盯着。

**⑤ 选项区上提加大**
正文最多占 60vh，选项区离底边 8vh，落在屏幕 65%~85%。字号 15→16.5px，内边距加大，序号做成小方章（对应键盘数字键）。

**⑥ 顶栏图标分组**
左组游戏功能（情报 / 回顾 / 存档 / 音乐），竖线分隔，右组系统功能（反馈 / 设置 / 首页）。

**⑧ 主角常驻面板**
左栏拆成三个独立区块：

```
┌─ 主角卡 ────────┐
│ ┌──┐ 张明        │  头像 62×62，右下角表情徽标
│ │头│ 状态正常     │  没有立绘时显示「张」字兜底
│ └──┘ ▓▓▓▓▓ 体况  │
├─ 随身物品   2 ──┤
│ ▪ 斧头          │
├─ 生存状态 ──────┤
│ 💧 水源    充足  │
│ 🍱 食物    不足  │
│ 📁 逃生情报 ▪▪▫▫ ›│  ← 可点开
├─ 队伍     2人 ──┤
│ ◆ 老孙          │
└────────────────┘
```

表情系统留好了：`HERO.faces` 登记六种表情的图片路径，图放进 `images/` 自动生效。身体状况 0~100 分四档，配色跟着变。

NPC 立绘还在右下角（`#portrait-layer`），跟主角卡是两个位置。

**顺手修的两个真 bug**（都是写测试时暴露的）

1. 打字期间 `_currentChoices` 还留着上一节点的选项，此时按数字键会跳到上一节点的分支
2. 锁住的选项只是界面禁用，按数字键能强行触发

**⑦ 关于 `_tests/`**
是，全都是测试代码，不参与游戏运行。游戏就是 `末日求生_第一章.html` 一个文件，单独拷走也能玩。

### 第一轮

- 存档：1 自动位 + 6 手动位，读/写/重命名/删除，抽屉带滚动
- 概率：`successRate` 数字 → `tier` 三档，区间内随机取阈值
- 音效：`sfxDelay` 毫秒延迟 → 文字内嵌 `[[sfx:name]]` 标记
- 章节：删掉节点里 33 处 `chapter:` 和 `bgm:`，靠 id 前缀自动推导
- BGM：绑在章节上，每次渲染节点都确认在播，读档/回顾/切标签页都不会丢
- 老孙线分流：`JOIN_GIRL_EVENT` 常量按队伍状态自动分流
- 状态自动检测：文字里写「获得逃生信息+1」引擎自己加
- 移动端适配移除，改 PC 全屏沉浸式剧场 + 悬浮 HUD
- 剧情回顾、跳过已读、反馈区、测试面板

修掉的 bug：`c1_open_door_roll` 的 `successNext` 原本指向老孙被咬的坏结果（语义反了）；四个节点情报被算两次。

---

## 四、待办

### 待写正文（9 个节点）

跑 `node _tests/_integrity.js` 会列出来。机制部分（入队、扣体况、条件块位置、音效位）都接好了，把 `TODO：…` 换成你的文字即可，条件块可增可删。

| 节点 | 是什么 |
|---|---|
| `c1_girl_event` | 2 处：知情/不知情两版心理描写 |
| `c1_girl_saved_sun` | 6 处：A 成功·老孙在场（三线共用，含有/无斧头对照） |
| `c1_girl_saved_solo` | 6 处：A 成功·单人（含夜/昼对照） |
| `c1_girl_rescue_fail_sun` | 3 处：A 失败但老孙救你 |
| `c1_girl_lure_ok` | 5 处：B 成功引开 |
| `c1_girl_lure_fail` | 5 处：B 失败 |
| `c1_girl_ignore` | 3 处：C 不管 |
| `c1_die_girl_rescue` | 2 处：A 失败孤身死亡结局 |
| `c1_ending` | 7 处：第一章收尾，按队伍/情报分岔 |

### 素材缺口

`images/` 目录还不存在，以下引用都会 404（有兜底，不会破图）：

```
images/title_bg.jpg              标题页背景
images/apartment_building.jpg    第一章场景背景
images/hero_normal.png 等 6 张    主角表情立绘
images/portrait_sun.png          老孙立绘
images/portrait_lin.png          林晓雨立绘
```

现在的兜底是 CSS 生成的废土底景（冷色天光 + 斜向锈痕 + 地平线微光），主角头像显示「张」字。放进图就自动替换。

### 结构性的事

- `c2_start` 不存在，`c1_ending` 指向它 → ②会一直报这一个错误
- `c1_girl_event` 的夜晚守夜开场提到"抱着那把斧头"，被 `flag:"night"` 守着，被动加入线不会显示。但白天线也有斧头却看不到这段——想让白天线也提一句斧头，那里得再加条件块
- 失败分支 `c1_girl_rescue_fail_sun` 两条线共用，斧头有无不影响失败描写。想区分"有斧头还失败"和"只有菜刀失败"，节点里加 `{ item:"斧头", text:…, else:… }`
- 资源系统还是摆设：水/食只被赋值，没有任何逻辑读它们。要让它有意义，可以让选项 `requires: { item:"…" }`、或让情报数量影响 `tierIf` 提档
- 反馈云端上报：`FEEDBACK_CONFIG.endpoint` 填上表单地址就自动切成云端提交，留空走本地导出
