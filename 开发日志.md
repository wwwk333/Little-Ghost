# 末日求生 · 寂静居民楼 —— 开发日志（正式版 v1.0）

对应文件：`末日求生 · 寂静居民楼 正式版.html`。

本日志取代旧版开发笔记。旧日志里描述的部分机制（情绪自动判断 MOOD_RULES、体况系统、冲出去救林晓雨的三档判定等）**在正式版中已移除或重做**，一切以本文为准。

## 目录

- [零、正式版现状](#零正式版现状)
- [一、怎么写剧情](#一怎么写剧情)
- [二、怎么调试](#二怎么调试)
- [三、正式版变更记录](#三正式版变更记录)
- [四、待办](#四待办)

---

## 零、正式版现状

第一章已完整写完，单 HTML 文件，无任何外部 JS 依赖。

| 项目 | 数量 / 状态 |
|---|---|
| 剧情节点 | 48 个（含 5 个概率判定节点） |
| 死亡结局 | 5 个：天降之喜 / 迎面而来 / 存活？/ 勇气是人类的赞歌 ×2 |
| 章节结局 | 2 个：留守线 `c1_end_ally`、救助线 `c1_end_thrill` |
| 隐藏彩蛋 | 1 个：求生无门（开局「回看对面阳台」） |
| 逃生情报 | 5 条：sound / vision / eastgap / light / fever |
| 概率档位 | low（28~42%）/ mid（48~62%）/ high（72~86%），区间内每次随机取阈值 |
| 装备耐久 | 仅斧头（上限 6 点，本章最多消耗 4 点，不会损坏） |
| 入队角色 | 老孙、周舟、赵大勇、刘阳、陈亮、王林雪 |
| 周舟好感 | 救助线专用，阈值 10（`ZHOU_JOIN_FAVOR`） |
| 存档 | 1 自动位 + 1 快速位（⚡）+ 5 手动位 |
| 结局收集 | 跨存档持久化（localStorage） |

分线结构：开场（检查门窗 / 打电话 / 呼救）→ 清点物资 → 老孙敲门 → 林晓雨事件 → 对面楼求助 → **留守线**（`stay_in_building`）或**救助线**（`went_opposite`）→ 林晓雨家（卧室电子锁）→ 夜晚支线 → 第一章完 → 鸣谢页。

---

## 一、怎么写剧情

所有内容都在 `const NODES = {` 那一段，配置在它上面。章节、背景、音乐不用写，引擎按节点 id 前缀自动归入 `CHAPTERS`。

### 1. 一个最简单的节点

```js
"c1_例子": {
  text: `<p>这里是正文。</p>
         <p>一段一个 &lt;p&gt;，玩家点一下出一段。</p>`,
  choices: [
    { key:"A", text:"选项文字", next:"下一个节点id" },
  ]
},
```

**不需要写的东西**（引擎自动处理）：章节名、背景图、背景音乐。节点 id 以 `c1_` 开头就自动归入第一章，从 `CHAPTERS.c1` 取默认背景 `images/apartment_building.jpg` 和 BGM `c1_background_dark.mp3`。某节点想换背景/音乐，写 `bg:` / `bgm:` 覆盖即可。

其他常用字段：

| 字段 | 作用 |
|---|---|
| `autoSavePoint: true` | 到达该节点自动存到「自动存档」位 |
| `stateChange: {...}` | 见第 7 节 |
| `chapterSummary: true` | 章末结算节点专用，正文打完后追加结算文字 + 「结束第一章」按钮 |
| `isEnding` 系列 | 见第 10 节 |

### 2. 文字样式

| 写法 | 效果 |
|---|---|
| `<p>普通</p>` | 正文 |
| `<p class='narr-scene'>…</p>` | 灰色斜体，环境描写 |
| `<p class='narr-thought'>…</p>` | 米白斜体，内心话 |
| `<p class='narr-dialogue'>…</p>` | 自动补中文引号“” |
| `<p class='narr-important'>…</p>` | 偏红加粗，重点 |
| `<p class='narr-hint'>【…】</p>` | 黄铜色带左边线，给玩家的提示 |
| `<p class='narr-sys'>…</p>` | 系统提示（资源变化等），引擎注入用 |

### 3. 音效

在文字里插标记，打字机打到那个字才播，玩家翻多快都能对上：

```js
text: `<p>[[sfx:knock]]"咚咚咚！"有人敲门。</p>
       <p>突然，"啊！"[[sfx:scream]]楼上传来一声尖叫。</p>`,
```

段首 = 段落一开始播；插在句中 = 打到那个字才播。

正式版已登记的音效（`const SFX`）：

```
tv   电视雪花    clock   挂钟      phone   电话铃
knock 敲门      scream  尖叫      drip    滴血/水声
footsteps 脚步  info    情报/道具提示音（引擎在拿到情报或道具时自动播，不用手写）
falling  坠落/巨响
```

加新音效：mp3 放进项目根目录，`SFX` 里加一行 `{ src: "xxx.mp3", vol: 0.5, dur: 6000 }`（`dur` 省略 = 播到自然结束；写了 = 到时间淡出）。

### 4. 概率判定

概率节点只写档位，不写数字：

```js
"c1_例子_roll": {
  isRoll: true,
  tier: "mid",              // low 凶险 / mid 五五 / high 稳妥
  successNext: "成功后的节点",
  failNext:    "失败后的节点",
},
```

正式版的 5 个判定点：

| 判定点 | 档位 | 内容 |
|---|---|---|
| `c1_knock_sun_roll` | mid | 去老孙家，楼道遭遇 |
| `c1_loud_noise_roll` | low | 呼救后阳台搏斗 |
| `c1_open_door_roll` | mid | 给老孙开门，是否被咬 |
| `c1_girl_home_danger_roll` | mid | 上楼遇感染者正面遭遇 |
| `c1_night_road_roll` | high | 救助线夜路硬闯（只有没斧头的人进得来） |

要整体调难度，改 `ROLL_TIERS` 三行区间，全部节点同步生效：

```js
const ROLL_TIERS = {
  low:  { label:"凶险", range:[0.28, 0.42] },   // 期望 ≈35%
  mid:  { label:"五五", range:[0.48, 0.62] },   // 期望 ≈55%
  high: { label:"稳妥", range:[0.72, 0.86] },   // 期望 ≈80%
};
```

每次判定在区间内随机取一个阈值再掷骰，所以同一档每次的实际成功率也有浮动。这也是"运气"的手感来源。

**让装备影响成功率**（`tierIf`，从上往下第一条命中的生效）：

```js
isRoll: true,
tier: "low",                                   // 基础：凶险
tierIf: [
  { item:"斧头", tier:"high" },                // 有斧头：稳妥
  { has:"老孙", tier:"mid"  },                // 有帮手：五五
],
```

### 5. 条件判定：所有能用的条件

下面这些字段能用在**三个地方**：`next` 分支、条件文本块、选项的 `requires` / `showIf`。

| 字段 | 含义 |
|---|---|
| `has:"老孙"` | 队伍里有这个人 |
| `notHas:"老孙"` | 队伍里没有 |
| `item:"斧头"` | 持有物品 |
| `notItem:"斧头"` | 没有该物品 |
| `knows:"sound"` | 已掌握某条情报 |
| `notKnows:"sound"` | 尚未掌握 |
| `intelMin:2` | 情报条数下限 |
| `flag:"went_opposite"` | 剧情开关已置 |
| `notFlag:"stay_in_building"` | 剧情开关未置 |
| `seen:"节点id"` | 玩家读过某节点 |
| `favorMin:10` / `favorMax:9` | 周舟好感区间 |

多个字段写在一起 = 全部满足才算命中。

### 6. 分线只差一两句话：条件文本块

**不要为了一句台词复制整个节点。** `text` 可以写成数组：

```js
text: [
  `<p>大家都一样的开头。</p>`,

  // 二选一
  { has:"老孙",
    text: `<p>老孙也跑出来了。</p>`,
    else: `<p>你独自僵在原地。</p>` },

  // 满足才插入，不满足整段不出现（没有 else）
  { flag:"went_opposite", text:`<p>救助线的额外描写。</p>` },

  `<p>大家都一样的结尾。</p>`,
],
```

正式版大量用到这个写法：`c1_girl_event`（有/无斧头、有/无老孙）、`c1_girl_saved_fail`（有/无老孙开场）、`c1_night_stay`（有/无老孙整段分版）、`c1_rescue`（斧头/刀子/绕花坛三种进楼方式）等。

### 7. 状态变化

```js
stateChange: {
  water: "充足",           // 水源（同时解锁左栏显示）
  food:  "充足",           // 食物
  join:  "老孙",           // 入队，可写数组 ["周舟","赵大勇","刘阳"]
  leave: "老孙",           // 离队
  item:  "斧头",           // 获得物品
  itemLose: "菜刀",        // 失去物品
  intel: ["sound"],        // 掌握情报（也可走 INTEL_SOURCES 登记）
  flag:  "went_opposite",  // 打剧情开关（分线标记都走这里）
  zhouFavor: 5,            // 周舟好感，正负累加；≥10 自动入队（仅救助线判定）
},
```

选项上也能写 `stateChange`，选了才生效（如 `c1_night_opposite` 的选项打 `night_hard_way` 标记）。

### 7b. 装备耐久

登记在 `ITEM_DURABILITY` 里的装备才有耐久：

```js
const ITEM_DURABILITY = {
  "斧头": { max: 6, breakText: "" },   // breakText：损坏时弹的提示
};
```

剧情里掉耐久，**在正文里写一行就行**，不用碰 `stateChange`：

```js
text: `<p>斧头落下，血花飞溅……</p>
       <p class='narr-sys'>斧头耐久 -2</p>`,
```

`AUTO_STATE_RULES` 会自动识别「斧头耐久 -2」「耐久值-3」「耐久 减 1」等写法并扣数（同一节点只结算一次）。左栏「生存状态」区出现「斧头耐久」行，显示 `4/6`，低于 1/3 转黄、剩 1 点转红。

**扣到 0 自动损坏**：移出背包（`requires:{item:"斧头"}` 随之锁住、`notItem` 分支成立），物品栏留一条划掉的「已损坏」记录。重新拿到同名装备耐久回满。

> 注意：损坏会改变背包内容，而条件文本块依赖背包判断。引擎在状态结算时就把正文**定稿**一次（`_composedText`），`renderNode` 直接用，不会"前脚写斧头落下、后脚翻成一刀砍在"。

### 8. 情报：加一条新线索

```js
const INTEL_DB = {
  你的key: {
    icon: "🔧", title: "标题",
    detail: "玩家点开能看到的正文",
    use: "用途一句话",
  },
};
```

然后在 `INTEL_SOURCES` 里指定哪个节点给哪条（不用改剧情文字）：

```js
const INTEL_SOURCES = {
  c1_eye_cheek:       ["vision", "eastgap"],
  c1_loud_noise_safe: ["sound"],
  c1_to_door:         ["sound"],
  c1_girl_home:       ["light"],
  c1_rescue:          ["fever"],
};
```

玩家点左栏「逃生情报」那一行就能查阅。左栏不显示固定格子数，只报条数——以后加多少条线索都不用改 UI，也不剧透总数。

### 9. 锁线：装备/条件决定选项能不能选

```js
choices: [
  { key:"A", text:"一斧头砍过去!",
    next:"c1_girl_room_search",
    requires: { item:"斧头" },        // 没斧头就锁住
    lockText: "一斧头砍过去!",        // 锁住时仍显示这行文字
    lockHint: "缺少道具" },           // 右侧小标签
]
```

锁住的选项**会显示**，灰掉不可点，让玩家知道「这里本来有条路」。键盘数字键也绕不过去。

选项的去留用另一组字段（条件不满足时**整条不渲染**）：

| 字段 | 效果 |
|---|---|
| `flag:"stay_in_building"` | 只有留守线渲染 |
| `notFlag:"went_opposite"` | 救助线不渲染 |
| `showIf:{...}` | 完整条件（支持第 5 节全部字段） |
| `hidden:{...}` | 同 showIf，语义同上 |

正式版例：`c1_girl_room_search` 的分线选项、`c1_night_opposite` 的有/无斧头两版选项 A。

### 10. 结局与彩蛋

```js
"c1_die_xxx": {
  isEnding: true,
  endingType: "bad",            // bad → 红调压暗；good → 绿调
  endingName: "天降之喜",
  endingQuote: "开饭！",
  text: `...`,
}
```

到达结局节点自动：收录进「结局收集」（跨存档）→ 压结局色调 → 打完字出「重新开始」按钮。

彩蛋节点写法：

```js
"c1_help_girl": {
  isCollectible: true,
  collectTitle: "求生无门",
  collectDesc: "对面阳台上的女孩，你再次返回时，只看见血迹。",
  ...
}
```

章末结算节点加 `chapterSummary: true`，打完正文后引擎自动追加「第一章 · 结算」：随身物资（含耐久）、同行的人、掌握的情报、离开时状态、周舟好感、已解锁结局/彩蛋，全部由打字机打出来，不再是独立卡片 UI。

### 11. 共用片段：一次修改多处生效

正式版把三处重复结尾抽成了常量，改一处三处跟着变：

| 常量 | 用在哪 |
|---|---|
| `JOIN_GIRL_EVENT` | 四条开场线汇合到林晓雨事件。目前固定 `"c1_girl_event"`，保留是为了以后真要分流时有地方改 |
| `JOIN_NIGHT` | 卧室段结束按 `went_opposite` 开关自动分流到 `c1_night_stay` / `c1_night_opposite` |
| `BEDROOM_LOCK_TEXT` + `BEDROOM_DOOR_CHOICES` | 留守线、救助线 A、救助线 B 三个节点的卧室电子锁正文与选项（开门要斧头、耐久由 `c1_bedroom_loot` 正文统一结算） |

### 12. 人物档案

`CAST` 里登记入队角色的档案：立绘 `portrait`、姓氏字 `surname`（没图时兜底）、`mood {icon,label}`、`role`、`age`、`relation`、`status`、`tags`、`note`。左栏队伍列表每人一行，点开看档案，底部可在主角和队友间切换。主角档案点主角卡打开（随身物品 + 已掌握情报汇总）。

**没有立绘不会破图**：图片加载失败自动落回姓氏字 + 灰调底。

---

## 二、怎么调试

### 快捷键

| 键 | 作用 |
|---|---|
| `Ctrl+Shift+D` | 开关测试面板（需先开创作者模式） |
| `Ctrl+Shift+L` | 弹出独立日志窗口 |
| `I` / `R` / `S` / `L` / `F` | 情报 / 回顾 / 存档（保存模式）/ 读档 / 反馈 |
| `M` | 静音 |
| `H` | 返回首页 |
| `1` `2` `3` … | 直接选对应选项 |
| `Esc` | 关确认框 → 关抽屉 → 关测试面板 |

### 创作者模式

标题页右下角版本号 `v1.0` **连点 5 次**开启（写进 localStorage，永久生效）。未开启时按 `Ctrl+Shift+D` 只会提示未开启。

### 存档面板

一个面板两种模式，标题跟着变：

- **读取存档** — 点一下读档；空位置灰显示「无存档」，不可点
- **保存存档** — 点一下写入前弹确认框；已有内容显示「覆盖」

三个固定槽位：**自动存档**（只读，到达 `autoSavePoint` 节点自动写入，toast 有 1.5 秒节流）、**快速存档**（HUD 的 ⚡ / 📂，不可删除改名）、**手动存档 1~5**（✎ 重命名、✕ 删除）。

标题页点「读取存档」会临时把标题页压到游戏层之下、挂 `from-title` 藏掉游戏内 HUD，只留抽屉浮在标题画面上；关掉抽屉 `restoreTitleLayer()` 还原。

### 测试面板（Ctrl+Shift+D）

按使用顺序排：

1. **节点列表** — 搜 id 或正文，点一下直接跳（概率节点自动跳 successNext）。标注「概率 / 好结局 / 坏结局 / 读过」
2. **状态篡改** — 水、食、情报（填 key 逗号分隔）、队伍、物资（带耐久的自动补满）、剧情开关、周舟好感。好感数字旁实时提示差多少入队
3. **测试概率** — 见下节
4. **流程** — 「档位」强制所有判定用某一档（`tier` / `tierIf` 暂时失效）；「结果」强制成功/失败（不掷骰）。两层可叠加
5. **工具** — 节点连通检查（扫断链、空 next、孤儿节点、未登记音效）、打印状态
6. **运行日志** — 面板内小窗，或「弹出独立窗口」

### 测试概率：五步

1. **判定点** — 下拉列出全部 `isRoll` 节点，括号里是它当前实际用的档位（`tierIf` 提档时显示 `low→high`）
2. **本点档位** — 只改选中这一个节点的 `tier`，状态条会说清与源码的差异、有没有被强制档位盖掉
3. **各档区间** — 改 low/mid/high 的百分比，「应用区间」立刻作用于全部判定；「恢复默认」只退区间，不动各点档位
4. **掷骰子 / 测试抽样** — 掷骰子看单次结果；抽样用**真实 `rollTier` 跑 N 次**，对比实测率与期望值（进度条黄竖线是期望刻度）。抽样临时忽略「强制结果」，但照算「强制档位」
5. **保存修改 / 恢复默认** — 保存把「区间 + 各点档位」写进 localStorage，刷新自动套用；恢复默认两样一起回源码值。想写回代码点「生成代码」，复制 `ROLL_TIERS` 片段粘回 HTML

区间状态条三色：灰 = 源码原值，黄 = 改了但只在本局有效，绿 = 已保存。

### 掷骰流程：一次判定四步

代码在 `rollTier()`，选项点下去以后：

1. **定档** — `resolveTier()` 扫 `tierIf`，第一条命中的生效；都不命中用节点 `tier`；测试面板的强制档位优先级最高
2. **定阈值** — `threshold = lo + random()*(hi-lo)`，同档位每次浮动
3. **掷骰** — `dice = random()`，`dice < threshold` 即成功
4. **分流** — 成功走 `successNext`，失败走 `failNext`；`next` 都支持 `{ has:"老孙", then, else }` 再分

日志格式：

```
[ROLL] high 区间=72~86% 阈值=0.783 骰=0.412 → 成功
```

### 独立日志窗口（Ctrl+Shift+L）

拖标题栏移动，拖右下角缩放，`A+`/`A−` 调字号，`↵` 切换换行。位置、大小、字号都记住。日志按类型上色：掷骰(黄) / 状态(绿) / 错误(红)。

### 没有命令行测试了

正式版是单 HTML 文件，不依赖 `_tests/` 目录。连通性检查用测试面板里的「节点连通检查」按钮，结果打在日志窗里。

---

## 三、正式版变更记录

相对旧开发笔记（即上一版开发日志所描述的状态）的主要变化：

### 剧情全部写完

- 旧日志列的 9 个待写节点全部完成，第一章 48 个节点无 TODO
- 林晓雨事件重做：A「制造声音引开」改为**必然救不下**（引开感染者 → 她下楼逃跑 → 被扑倒），B「不管」；旧版"冲出去救她"的三档判定（空手 low / 斧头 mid / 斧头+老孙 high）**整组移除**
- 新增对面楼求助主线：白板对话 → 选「不管」进**留守线**、选「等天黑去帮忙」进**救助线**（`flag` 分线）
- 卧室电子锁段落抽出共用常量（见一·11），拿车钥匙（斧头耐久 -2）
- 夜晚支线两条线各自收束：留守线 601 结盟（周舟+赵大勇+刘阳入队 + 警棍）；救助线夜路（斧头硬闯 / 无斧头走花坛）→ 陈亮家 → 陈亮、王林雪入队
- 章末结算改为打字机纯文本（`chapterSummaryHTML`），不再是卡片 UI
- 结局接鸣谢页（两步），结尾 BGM 切 `the_end.mp3`，回标题页自动切回 `the_start.mp3`

### 情报 4 → 5 条

新增 `fever`（高烧也会变成它们），来自救助线陈亮家（王林雪告知）。`INTEL_SOURCES` 改为纯登记制，五个来源：`c1_eye_cheek`（vision+eastgap）、`c1_loud_noise_safe`（sound）、`c1_to_door`（sound）、`c1_girl_home`（light）、`c1_rescue`（fever）。

### 新增系统

- **周舟好感**：`zhouFavor` 状态 + `ZHOU_JOIN_FAVOR = 10` 阈值，只有救助线判定入队；`stateChange:{ zhouFavor:5 }` 累加，左栏救助线显示好感行
- **斧头耐久**：`ITEM_DURABILITY` 登记表，正文写「斧头耐久 -N」自动扣，损坏自动移出背包
- **结局收集**：`isEnding` / `isCollectible` 节点自动收录进 localStorage，跨存档，左栏「结局收集」行可点开查阅，有新条目时呼吸提示
- **好感/耐久 HUD 行**：左栏「生存状态」区按状态动态显示「斧头耐久」「周舟好感」行

### 移除的系统

- **情绪自动判断（MOOD_RULES）**：正式版没有 MOOD_RULES，也没有体况（condition）系统。情绪改为 `CAST` 里每人静态的 `mood:{icon,label}`，主角不显示情绪档
- `HERO.faces` 六表情立绘、节点级 `heroMood`、`stateChange:{mood:...}` 全部不存在，别按旧日志写

### 角色表

`CAST` 现有 6 人：老孙、周舟、赵大勇、刘阳、陈亮、王林雪。林晓雨没有入队（剧情中死亡），不在 CAST。新增角色照 `CAST` 里任意一条的字段抄。

### 音效

名单变更：`steps` → `footsteps`；新增 `info`（情报/道具提示音，引擎在拿到情报或道具时**自动播**，正文不用手写）、`falling`（楼上巨响）。旧日志提到的四个信息点手挂 `[[sfx:info]]` 的写法不再必要（自动触发），但写了也不冲突。

### 反馈

`FEEDBACK_CONFIG.endpoint` 已填 Formspree 地址（`https://formspree.io/f/mljdllro`），提交时自动 POST 云端，失败回落本地 + 复制/下载导出。

### 标题页

只留「开始游戏 / 读取存档」两个入口（无存档时显示闪烁的「—— 开始游戏 ——」）。版本号 v1.0 兼任创作者模式开关（连点 5 次）。

### 存档面板

- 手动槽位 6 → 5，文案不再提槽位数
- 「保存后返回首页」模式：存完自动回标题页
- 修掉标题页读档点了没反应（抽屉层级比标题页低 → 标题页压到 z-index 5 + `from-title` 藏 HUD）

### 测试面板

- 新增「本点档位」：只改选中那一个判定点的 `tier`（区别于「流程」里把所有判定钉成一档的强制档位）
- 「保存修改」把区间 + 各点档位一起持久化；「恢复默认」一起回源码
- 新增节点档位持久化（`NODE_TIER_KEY`）

### 其他修复

- 打字期间按数字键误触发上一节点选项、锁住选项被数字键绕过的 bug（旧日志第二轮已修，此处保留记录）
- 耐久归零把装备移出背包后，条件文本块二次 compose 导致前后文矛盾 → 正文定稿机制（`_composedText`）
- 自动存档 toast 1.5 秒节流
- 页面隐藏时暂停 BGM，回来续播

---

## 四、待办

### 素材缺口

`images/` 目录还不存在，以下引用都会 404（有兜底，不会破图）：

```
images/title_bg.jpg              标题页 / 鸣谢页背景
images/apartment_building.jpg    第一章场景背景
images/hero_normal.png           主角立绘（人物档案顶部）
images/portrait_sun.png          老孙立绘（右下角对话立绘 + 档案）
images/portrait_zhou.png         周舟立绘
```

兜底表现：背景是 CSS 生成的废土底景（冷色天光 + 斜向锈痕 + 地平线微光），头像/立绘显示姓氏字。放进图就自动替换。

赵大勇、刘阳、陈亮、王林雪四人没配 `portrait` 字段，档案顶部直接显示姓氏字；要加立绘在 `CAST` 对应条目补 `portrait:`。

### 第二章

- 两个章节结局节点（`c1_end_ally` / `c1_end_thrill`）目前是「未完待续」，鸣谢页后直接回标题
- 要写第二章：在 `CHAPTERS` 加 `c2`（含 `match: [/^c2_/]`），节点 id 用 `c2_` 开头即可自动归入

### 结构性的事

- **周舟好感目前攒不满**：救助线只在 `c1_zhou_meet_friendly` 给 +5，到不了入队阈值 10，`syncZhouJoin()` 第一章内不会触发。要么第二章补好感事件，要么把阈值降到 5——当前周舟在救助线**不会入队**，别在第二章文案里默认他在队
- **水/食物仍是显示层**：只被赋值和展示，没有任何逻辑读它们。想让它有意义，可以拿 `intelMin` / `item` / `flag` 做门槛，或加资源消耗事件
- **斧头本章不会损坏**：全章最多消耗 4 点（上限 6）。`ITEM_DURABILITY` 和损坏机制都已就绪，第二章加高消耗场景即可
- **两个「勇气是人类的赞歌」**：`c1_girl_room_fail` 和 `c1_die_night_road` 同名不同节点，结局收集里会显示成两条同名条目。想区分就给其中一条改名
